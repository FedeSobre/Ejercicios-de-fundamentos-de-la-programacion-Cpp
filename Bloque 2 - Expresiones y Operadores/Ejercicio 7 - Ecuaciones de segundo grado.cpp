////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Bloque 2 - Ejercicio 7

/* Escriba un programa que calcule las soluciones de una ecuación de segundo grado de la forma ax^2 + bx + c = 0, 
teniendo en cuenta la definicion de la formula resolvente (o formula de Bhaskara):
(-b + √(b² - 4 * a * c)) / (2 * a) y 
(-b - √(b² - 4 * a * c)) / (2 * a)
*/

#include <iostream>
#include <math.h>

using namespace std;

int main() {

	float a, b, c;
	
	return 0;
}